name: Kernel Build (lineage-21 + SukiSU Ultra)

on:
  workflow_dispatch:
  push:
    branches:
      - lineage-21

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout source (lineage-21)
      uses: actions/checkout@v4
      with:
        ref: lineage-21

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          bc bison build-essential ccache curl flex git \
          libncurses5-dev libssl-dev \
          gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi \
          python3 unzip zip

    - name: Download Proton-Clang
      run: |
        git clone https://github.com/kdrag0n/proton-clang.git --depth=1 $HOME/proton-clang

    - name: Apply KernelSU (SukiSU Ultra)
      run: |
        curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s nongki

    - name: Set environment
      run: |
        echo "PATH=$HOME/proton-clang/bin:$PATH" >> $GITHUB_ENV
        echo "ARCH=arm64" >> $GITHUB_ENV
        echo "SUBARCH=arm64" >> $GITHUB_ENV
        echo "CC=clang" >> $GITHUB_ENV
        echo "CROSS_COMPILE=aarch64-linux-gnu-" >> $GITHUB_ENV
        echo "CROSS_COMPILE_ARM32=arm-linux-gnueabi-" >> $GITHUB_ENV

    - name: Build kernel
      run: |
        mkdir -p out
        make O=out sakura_defconfig
        make O=out -j$(nproc) KCFLAGS=-Wno-error

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: Image.gz-dtb
        path: out/arch/arm64/boot/Image.gz-dtb
